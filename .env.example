# 复制成 .env 并填入真实值。.env 已在 .gitignore 里，不会被提交。
# `npm run dev` 会自动加载 .env。

# --- Google Maps Platform（ADR-008 数据源 + ADR-007 地理编码）---
# 一个 key 同时用于 Places API (New) 和 Geocoding API。只在 Edge Function 环境变量里用。
# 【硬约束】启用当天就去 Cloud Console → Quotas 设每日请求数上限：
#   SearchNearbyRequest → 200/天，GetPhotoMediaRequest → 300/天，Geocoding → 300/天，其余压到 1。
#   预算告警只发邮件不阻断计费，配额上限才是熔断器。
# 没填时，后端回落到演示数据（supabase/functions/_shared/mock/google-places.json）。
GOOGLE_PLACES_API_KEY=

# --- Supabase（部署 Edge Function 用）---
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_REF=

# --- 应用层 token ---
# 设了之后，/search、/geocode 都要求请求带 X-App-Token 头且匹配。
# 部署时设成 Supabase secret；本地留空即可（不校验）。随机生成：openssl rand -hex 24
APP_TOKEN=
