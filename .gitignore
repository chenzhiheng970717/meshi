# === 凭据：绝对不能进仓库 ===
# API Key 一旦进了 git 历史，删掉文件也没用——历史里还在。
# 真泄漏了只能去 Recruit / Supabase 后台重新生成。
.env
.env.*
!.env.example
*.pem
*.key
secrets/
.supabase/

# === HotPepper 真实数据：不得落库 / 复制（Recruit 利用規約，缓存 ≤ 24h）===
# 抓取的真实店铺数据只留在本地做解析调试，不进仓库。
fixtures/open-strings.json
fixtures/*-real.json

# === 依赖与构建产物 ===
node_modules/
dist/
build/
.vite/
.turbo/
*.tsbuildinfo

# === 日志与缓存 ===
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.cache/
coverage/

# === 系统与编辑器 ===
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~
